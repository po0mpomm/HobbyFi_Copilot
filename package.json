{
  "name": "HobbyFi_Copilot",
  "version": "1.0.0",
  "description": "HobbyFi Copilot Monorepo",
  "scripts": {
    "dev": "pnpm --parallel --recursive run dev",
    "seed": "pnpm --filter db exec npx tsx src/seed.ts",
    "db:push": "pnpm --filter db exec npx prisma db push",
    "db:studio": "pnpm --filter db exec npx prisma studio"
  },
  "packageManager": "pnpm@10.33.2",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "sharp",
      "unrs-resolver"
    ]
  }
}
